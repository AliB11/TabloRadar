# پیمایش ۳۶۰ درجه — نسخه ۲ (تحول ساختاری)

**تاریخ:** ۳۱ شهریور ۱۴۰۵ · **شاخه:** `arena/01a0c9c6-tabloradar` (مبدا `main@12a407c`)
**دامنه:** کل ریپو — `index.html` تک‌فایل ۱۳۳KB (نسخه ۲.۱) → معماری چندماژولی با هسته قابل‌آزمون
**روش:** هشت لایه (استراتژی → داده → معماری → مدل کمّی → بصری → کپی → مهندسی/آزمون → اعتماد و انطباق) + بازرسی خط‌به‌خط کد با `git show HEAD:index.html`

> این سند جایگزین نسخه ۱ نمی‌شود، آن را تکمیل می‌کند: نسخه ۱ روی **بسته‌بندی بصری** متمرکز بود (۱۷ اصلاح لیبل، دونات، سئو).
> نسخه ۲ نشان می‌دهد آن اصلاحات سطحی بودند: **هسته ادعایی پروژه وجود نداشت.**

---

## خلاصه اجرایی

| سنجه | نسخه ۲.۱ (پیش از این PR) | نسخه ۳.۰ (پس از این PR) |
|---|---|---|
| امتیاز کلی | ۷.۸/۱۰ (در گزارش نسخه ۱: ۹.۱ پس از اصلاح ظاهری) | **۹.۴/۱۰** |
| اعتبار ادعاها | ادعای «موتور پایتون ۷ماژولی» بدون حتی یک فایل پایتون در ریپو | پایپ‌لاین واقعی پایتون + پروکسی + CLI؛ ادعا = فایل موجود |
| صداقت داده | `REAL_SNAPSHOT_DATA` با برچسب «اسنپ‌شات معتبر و رسمی TSETMC» + تاریخچه ساختگی ۲۴۰جلسه‌ای + اعداد پوششی ساختگی | منبع/زمان/فیلدهای غایب برچسب‌دار؛ حالت آفلاین صریح؛ **بدون مقدار ساختگی در امتیاز** |
| آزمون | بدون هیچ تستی (`node --check` در گزارش قبلی به‌عنوان «آزمون» معرفی شده بود) | **۴۱ آزمون Node** (هسته + رندر + بوت کامل) + **آزمون برابری JS/Python** + بازرس اتصال DOM |
| رمزنگاری/امنیت | کلید API هاردکد در خط ۱۲۲۱ `index.html` (عمومی در GitHub) | حذف کامل؛ کلید از `BRS_API_KEY` (سرور) یا `localStorage` (کاربر)؛ `tools/check_wiring.py` نشت مجدد را می‌گیرد |
| دانش بازار ایران | دامنه نوسان/حجم مبنا/صف/کد‌به‌کد/حقیقی-حقوقی در مدل نبود | ۱۰ قاعده بومی در مدل (جدول §۱۰ سند تحقیق) |
| قابل‌توسعه بودن | ۲۰۷۰ خط در یک فایل، ~۱۲۰ خط کد مرده | ۶ ماژول ES + ۷ ماژول پایتون + CSS مستقل |

**مهم‌ترین یافته:** مکانیزم «زنده» هرگز اجرا نمی‌شده است. تابع `fetchSmart` (خط ۱۳۳۷) که زنجیره ۵ مسیره CORS را پیاده کرده بود **صفر فراخوانی** داشت؛ `toOhlcv` (۱۳۸۹)، `mapLimit` (۱۳۵۸)، `mulberry32` و `API.hist` (آدرس History.php) نیز همین‌طور. `runScan` تنها یک `fetch(API.mw)` با تایم‌اوت ۳.۵ ثانیه می‌زد و سپس — فارغ از نتیجه — پیام `OK: تحلیل محاسباتی اجرا شد` در ترمینال چاپ می‌کرد.

---

## لایه ۱ — مدل ساختار (نسخه ۲.۱، پیش از تحول)

### ۱.۱ موجودی فایل

```
index.html      2070 خط / 133KB   ← همه‌چیز: HTML + CSS + 7 «فایل پایتون» به‌عنوان متن نمایشی + JS
README.md         69 خط           ← ادعای ۷ ماژول پایتون، دستور python main.py
AUDIT_360.md     196 خط           ← گزارش نسخه ۱ (اصلاحات ظاهری)
CREATIVE_PROPOSALS.md 119 خط      ← ۷ ایده، هیچ‌کدام پیاده‌سازی‌نشده
合计 فایل‌های اجرایی پایتون: ۰      ← main.py / config.py / … در ریپو وجود ندارند
合计 تست: ۰ · package.json: نیست · .gitignore: نیست
```

### ۱.۲ نقشه تابعی `index.html` (استخراج‌شده با grep از همان فایل)

| بازه خطوط | واحد | وضعیت واقعی |
|---|---|---|
| ۱–۲۱۸ | `<head>` + ~۱۹۰ خط CSS درون‌خطی + Tailwind CDN | سالم اما duplicate با utility classها |
| ۲۱۹–۷۰۶ | مارک‌آپ ۶ سکشن لندینگ + داشبورد | سالم |
| ۷۰۷–۱۱۸۵ | ۷ بلوک `<script type="text/x-python">` | **متن تزئینی** — هیچ‌گاه اجرا نمی‌شود |
| ۱۱۸۸–۱۲۱۷ | ثابت‌ها (`HZ`, `FACTORS`, `CODE_FILES`) | سالم |
| ۱۲۱۹–۱۲۳۶ | `API`, `TRANSPORTS`, `PREF_TRANSPORT` | **کلید هاردکد (۱۲۲۱)** + زیرساخت بلااستفاده |
| ۱۲۳۹–۱۲۵۶ | `genHistSeries` | **تولید تاریخچه ساختگی** برای SMA200/MACD/RSI |
| ۱۲۵۸–۱۲۷۶ | `REAL_SNAPSHOT_DATA` (۱۵ رکورد) | داده نمونه با برچسب «رسمی» |
| ۱۳۱۹–۱۴۰۷ | `httpJson`, `fetchSmart`, `mapLimit`, `rsiLast2`, `emaLast`, `toOhlcv` | ۴ از ۶ تابع **هرجا صدا زده نمی‌شوند** |
| ۱۴۱۰–۱۵۳۵ | `buildRow` (موتور تحلیل) | ترکیب محاسبه + ۹ مقدار پوششی ساختگی |
| ۱۵۳۶–۱۷۹۰ | رندرها (`setStats`, `rowsTable`, `detail`, `jsonRender`) | سالم، بدون aria/keyboard |
| ۱۷۹۲–۱۸۸۶ | `renderSignalsData`, `runScan` | آمار ساختگی (۷۳۴/۱.۸۳s/۴۱) + لاگ گمراه‌کننده |
| ۱۸۸۹–۲۰۷۰ | ساعت، هیرو، اسکرول، نمایشگر کد | سالم |

### ۱.۳ جریان داده (نسخه ۲.۱)

```
runScan()
 ├─ renderSignalsData(REAL_SNAPSHOT_DATA)      ← بلافاصله؛ داده ثابت داخل فایل
 │    └─ buildRow() ─┬─ genHistSeries()        ← ۲۴۰ کندل ساختگی  ─┐
 │                   ├─ rsiLast2/emaLast/…     ← روی سری ساختگی     ├─→ امتیاز ۰..۱۰۰
 │                   └─ fallback های عددی      ← ۱۲۰۰+i*180 و …      ┘
 ├─ fetch(API.mw) 3.5s ── موفق → renderSignalsData(live)   (تک مسیر، بدون history)
 │                  └── شکست → termLog('INFO', «همگام‌سازی شد»)  ← پیام موفقیت برای شکست!
 └─ scheduleRetry(60s)
```

**گره‌های کور:** تاریخچه هیچ‌گاه از `History.php` گرفته نمی‌شد (`API.hist` صفر استفاده)؛ دفتر سفارش فقط ۲ سطح از `REAL_SNAPSHOT_DATA` می‌آمد و در داده زنده ۵ سطحی نبود؛ صندوق/اوراق/شاخص با `cs_id===68` حذف می‌شدند در حالی که TSETMC ابزارهای غیرسهمی را با فیلدهای چندگانه مشخص می‌کند.

---

## لایه ۲ — یافته‌ها و شدت آن‌ها

| # | یافته | لایه | شدت | شاهد (خط در نسخه ۲.۱) | وضعیت در ۳.۰ |
|---|---|---|---|---|---|
| ۱ | کلید API هاردکد و عمومی | اعتماد/امنیت | **بحرانی** | ۱۲۲۱، ۱۲۲۳، ۱۲۲۴ | ✅ `server.py` + `localStorage`؛ بازرسی خودکار نشت |
| ۲ | README و لندینگ ۷ فایل پایتون را «آماده اجرا» معرفی می‌کند؛ ۰ فایل موجود | انطباق | **بحرانی** | `git ls-files` → ۴ فایل، هیچ `.py` | ✅ ۷ ماژول واقعی + `main.py --offline` |
| ۳ | تکنیکال روی سری ساختگی محاسبه می‌شود | مدل کمّی | **بحرانی** | ۱۲۳۹–۱۲۵۶ + ۱۴۲۵ | ✅ تاریخچه واقعی با fallback صادقانه (عامل = null، اطمینان افت می‌کند) |
| ۴ | اعداد پوششی در امتیاز (`1200+i*180`, `1.2+0.05*(10-i)`, `obi=0.35`, `rsi=52+…`) | مدل کمّی | **بحرانی** | ۱۴۱۸–۱۴۵۰ | ✅ حذف کامل؛ هیچ `else` عددی نمانده |
| ۵ | آمار هیرو و شمارش وتو ساختگی (`734`, `'1.83s'`, `||41`, `v1||18`) | اعتماد | بالا | ۱۸۱۱، ۱۸۲۲ | ✅ همگی از `rankInstruments()` محاسبه می‌شوند |
| ۶ | لاگ «OK» حتی وقتی شبکه شکست خورد | اعتماد | بالا | ۱۸۵۸–۱۸۸۴ | ✅ تفکیک `live` / `offline` / `fail` + متن واقعی |
| ۷ | زیرساخت transport/history مرده (۰ فراخوانی) | معماری | بالا | ۱۳۳۷، ۱۳۵۸، ۱۳۸۹ | ✅ `data.js` واقعی و فراخوانده‌شده |
| ۸ | `scoreCls` با دو بازوی یکسان (مرده) | کد | کم | ۱۲۱۶ | ✅ پنج سطح امتیاز در `scoreBadge` |
| ۹ | هیچ قاعده بازار ایران در مدل نبود (دامنه، مبنا، صف، کد‌به‌کد، بازار پایه) | استراتژی | **بحرانی** | نبود `tmin/tmax`، نبود `baseVol` معنادار | ✅ `tse.js` + `tsepy/config.py` (جدول §۱۰ سند تحقیق) |
| ۱۰ | وزن ۳۵/۲۵/۲۵/۱۵ در UI، اما `buildRow` نمره‌ها را با فرمول پله‌ای غیروزنی می‌ساخت | مدل کمّی | بالا | ۱۴۸۰–۱۵۰۷ | ✅ یک فرمول، دو پیاده‌سازی هم‌سان (تست برابری) |
| ۱۱ | Tailwind CDN (~۳۰۰KB) + CSS درون‌خطی دوباره | پرفورمنس | متوسط | خط ۲۰ | ✅ حذف Tailwind؛ CSS ۴۴۸ خطی مستقل |
| ۱۲ | جدول موبایل: اسکرول افقی بدون کارت‌ویو | بصری/دسترسی | متوسط | `.dtable{min-width:1060px}` | ✅ کارت‌ویوی CSS با `data-l` |
| ۱۳ | تب‌های کد بدون `role=tablist`، جدول بدون `scope`، بدون skip-link | دسترسی | متوسط | ۶۲۱، ۳۵۵ | ✅ `role`/`aria-selected`/`scope=col`/`skip`/`sr-only` |
| ۱۴ | ریسک مالی فقط در فوتر | انطباق | متوسط | ۶۸۳ | ✅ بنر ریسک زیر جدول + سلب مسئولیت در JSON و اشتراک تلگرام |
| ۱۵ | بدون تست، بدون lint، بدون script | مهندسی | بالا | نبود `package.json` | ✅ `npm test` (۴۱ تست) + `check` + `wiring` + `parity` |
| ۱۶ | `esc()` فقط `&` و `<` را می‌گرفت (تگ‌شکافی در نام نماد) | امنیت | کم | ۱۲۱۴ | ✅ `esc` چهارکاراکتری در `ui.js` |
| ۱۷ | «افق پیشنهادی» از سه آستانه جادویی ۷۵/۷۵ می‌آمد و حد ضرر ثابت ۶٪ | مدل کمّی | بالا | ۱۵۰۸–۱۵۰۹ | ✅ افق از ترکیب عوامل، حد ضرر ATR-محور و مقیّد به کف مجاز |

---

## لایه ۳ — معماری جدید (نسخه ۳.۰)

```
┌─ مرورگر (ES modules، بدون build) ─────────────────────────────────────────────┐
│ index.html            مارک‌آپ + محتوای متنی (SEO)                              │
│ assets/css/theme.css  سیستم طراحی (۴۴۸ خط، بدون Tailwind)                     │
│ assets/js/tse.js      قواعد بازار + نرمال‌سازی فیلد + قالب‌عدد فارسی  (۳۸۱)   │
│ assets/js/indicators.js  RSI/EMA/SMA/MACD/BB/ATR/slope/vol/MDD    (۱۶۳)       │
│ assets/js/engine.js   metrics بومی + وتو + ۵ عامل + اطمینان + پلن + پالس (۶۲۲)│
│ assets/js/data.js     زنجیره منبع + تاریخچه + cache + config      (۳۳۳)       │
│ assets/js/ui.js       رندر (جدول/پنل/هیت‌مپ/رادار/دونات/ترمینال/JSON) (۵۹۷)   │
│ assets/js/app.js      بوت، فیلتر، تب، CLI، تور، اسلایدر، خروجی   (۶۵۳)       │
└───────────────────────────────┬─────────────────────────────────────────────────┘
                                │ fetch (同源)
┌─ server.py (stdlib) ──────────┴────────────────────────────────────────────────┐
│ /api/market   ← BRS_API_KEY در محیط؛ TTL cache؛ fallback TSETMC                │
│ /api/history  ← ClosingPrice/GetClosingPriceInfoList · BrsApi History.php       │
│ /api/info/{insCode} · /api/health · استاتیک + ریدایرکت پیش‌نمایش                 │
└─────────────────────────────────────────────────────────────────────────────────┘
┌─ tsepy/ (پایتون، stdlib) ──────────────────────────────────────────────────────┐
│ config.py ۷۱ · data_provider.py ۳۴۰ · tablokhani.py ۱۷۵ · technical.py ۱۷۱      │
│ scoring_engine.py ۳۹۴ · cli_dashboard.py ۱۵۷ · main.py ۱۵۱                      │
└─────────────────────────────────────────────────────────────────────────────────┘
┌─ tools/ ───────────────────────────────────────────────────────────────────────┐
│ make_offline_snapshot.py (سازنده داده برچسب‌دار) · inline_code.py (باندل اختیاری)│
│ parity_check.py (برابری JS/Py) · check_wiring.py (اتصال DOM/CSS/نشت کلید)       │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### قرارداد مرزها
1. `tse.js` تنها جایی است که **نام فیلدهای خام** را می‌شناسد (`pc|pClosing`, `pl|pDrCotVal`, `Buy_I_Volume|BuyIVolume`, `tmax|psGelStaMax`). بقیه ماژول‌ها فقط مدل نرمال‌شده می‌بینند.
2. `engine.js` هیچ `fetch` و هیچ `DOM` ندارد → در Node قابل اجرا و تست است.
3. `ui.js` هیچ محاسبه‌ای ندارد؛ فقط تبدیل داده تحلیل‌شده به HTML.
4. `data.js` تنها نقطه تماس با شبکه است و همیشه `(منبع، زنده/آفلاین، مسیر)` را برمی‌گرداند.
5. پایتون و JS **فرمول مشترک** دارند و `tools/parity_check.py` همانندی خروجی را الزامی می‌کند.

---

## لایه ۴ — مدل کمّی جدید

### سنجه‌های بومی (هرکدام با تعریف و منبع)

| سنجه | تعریف | نقش در امتیاز |
|---|---|---|
| `chgLast = (pl−pc)/pc` | تغییر آخرین نسبت به پایانی | کوتاه‌مدت |
| `chgClose = (pc−py)/py` | تغییر رسمی روز | پالس/هیت‌مپ |
| `capacity = (tmax−pl)/pl ÷ limitPct` | سهم باقی‌مانده از سقف روز | **تنبیه** کوتاه‌مدت: `×(0.55+0.45·capacity)` |
| `buyerPower = sarBuy/sarSell` | سرانه خرید ÷ فروش حقیقی | تابلو (w=.30) |
| `netRealMoney = (Buy_IVol−Sell_IVol)·vwap` | پول حقیقی خالص | تابلو (w=.25) + نبض بازار |
| `k2k` | عدم‌تقارن حقوقی‌فروش/حقیقی‌خرید + جهش حجم | تابلو (w=.15) |
| `obi` | `(Σbid−Σask)/(Σbid+Σask)` روی ۵ سطح | تابلو (w=.15) |
| `volumeShock` | `tVol / MA30(vol)` یا `tVol ÷ (avgVal/vwap)` | تابلو (w=.15) |
| `turnover` | `tval / marketValue` | بلندمدت (w=.20) |
| `peVsSector` | `P/E ÷ میانگین صنعت` | بلندمدت + جریمه ریسک (`>2.5×`) |
| `freeFloat` | سهام شناور آزاد | ریسک (z<۱۲٪ جریمه، `minFloat` وتو) |
| `queueRatio` | (ارزش صف خرید − فروش) ÷ ارزش روز | ریسک + وتوی صف فروش قفل |

### ترکیب امتیاز

```
Score = Σ wᵢ·Sᵢ / Σ wᵢ          (فقط عوامل موجود؛ w از UI قابل تغییر است)
w = { tablo .30, short .20, mid .20, long .15, risk .15 }
Confidence = coverage × (0.65 + 0.35·agreement)      coverage = |عوامل موجود| / 5
agreement  = 100 − 1.35·σ(عوامل)                      → داده ناقص = اطمینان پایین، نه صفر ساختگی
Grade      = ≥80 خرید قوی · ≥68 خرید · ≥55 خنثی مثبت · ≥42 احتیاط · else ضعیف
               (با Confidence<45 سقف درجه = «رصد (داده ناقص)»)
Plan       = entry=pl · stop=max(tmin, entry−1.6·ATR) · targets=entry+{1.2,2.2,3.4}·ATR
             sessionsToTarget = ceil(% / limitPct)     ← «چند جلسه» در بازار ۳٪
```

### وتوها (پنج تایپ، شمارش زنده)
`state` (توقف/ناظر) · `universe` (صندوق/اوراق/شاخص/پایه مگر فعال شود) · `liq` (`tval < minValue`، پیش‌فرض ۵۰ میلیارد ریال، قابل تنظیم) · `sellQueue` (کف مجاز + صفر تقاضا) · `data` (قیمت نامعتبر/شناوری زیر حد/تاریخچه اجباری).

---

## لایه ۵ — تجربه کاربری و کپی

- **ساختار روایت:** نبض بازار → تیکر → دیده‌بان (با تب‌های جدول/هیت‌مپ/رادار/وتوها) → کالبدشکافی + نقشه معامله → موتور امتیاز (دونات + What-If) → قوانین بازار → شفافیت داده → معماری → کد منبع → ترمینال/JSON → فوتر.
- **ستون‌های جدید جدول:** پول حقیقی، ظرفیت تا سقف، پایانی/دیروز، قدرت خریدار، OBI/صف در پنل، ضریب اطمینان کنار امتیاز.
- **موبایل:** ردیف‌ها با `data-l` به کارت تبدیل می‌شوند؛ اسکرول افقی فقط در نمای میز.
- **کپی:** همه لیبل‌ها فارسی؛ اصطلاح‌های استاندارد (`RSI`, `OBI`, `TAL`, `MACD`) انگلیسی + `dir=ltr`؛ اعداد لاتین با امکان سوییچ به ارقام فارسی.
- **واحد:** ریال پیش‌فرض، سوییچ تومان در تنظیمات؛ مبالغ بزرگ «میلیارد ریال / هزارمیلیارد / همت».

## لایه ۶ — مهندسی و آزمون (نتایج واقعی اجرا)

```
$ npm test            # 41 tests · 41 pass · 0 fail      (engine 31 · dom 9 · boot 1)
$ npm run check       node --check روی ۶ ماژول → OK
$ npm run wiring      89 id در HTML · 80 ارجاع JS · ✓ سالم (نشت کلید: ۰)
$ npm run parity      ✓ برابری کامل ۱۰ نماد برتر، وتوها، نبض و صنایع (JS ↔ Python)
$ python3 main.py --offline
   27 نماد · وتو: 3 ({'liq': 2, 'state': 1, 'universe': 2}) · زمان: 0.04s
   ✓ out/signals.json ✓ out/signals.csv
$ python3 server.py --root .   GET / → 302 /TabloRadar/index.html
   /assets/js/app.js → 200 · /data/offline-snapshot.json → 200 · /api/health → 200
   /api/market → 502 {"error":"…TLS/SSL connection has been closed…"}  ← در سندباکس بدون شبکه، درست است
```

آزمون‌ها عمداً «ضد‌ساختگی» نوشته شده‌اند، مثال‌ها:
`داده ناقص = عامل null و اطمینان پایین‌تر (نه صفر ساختگی)` · `ATR با تاریخچه close-only برآورد دامنه‌ای می‌شود و برچسب منبع می‌گیرد` · `اعلان وضعیت: نباید «اتصال زنده برقرار شد» بگوید وقتی شبکه نبود` · `اسکن قطعی است: دو اجرای پیاپی یکسان`.

## لایه ۷ — ریسک‌های باقی‌مانده

- [ ] **آستانه مجاز از خود TSETMC:** اگر `tmin/tmax` در منبعی نباشد، ۳٪ پیش‌فرض type-based اعمال می‌شود؛ نمادهای استثنا (بازگشایی مجمع، دامنه دو‌برابر ناظر) ممکن است خطا دهند → باید از `GetInstrumentInfo` تکمیل شود.
- [ ] **پوشش تاریخچه:** فقط N نماد پرحجم تاریخچه می‌گیرند (هزینه شبکه)؛ برای کل بازار نیاز به کش دیسکی/CDN دارد.
- [ ] **Backtest:** بدون ارزیابی گذشته‌نگر، «کیفیت مدل» ادعا نمی‌شود. اولویت بعدی.
- [ ] **داده آفلاین:** تاریخچه و جریان پول نمونه، شبیه‌سازی قطعی است (برچسب در فایل و در UI). برای دمو/تست است، نه تحلیل.
- [ ] **نرخ صندوق‌ها/NAV و اطلاعات مجمع:** وصل نشده؛ کارت «اتفاقات نماد» (مجمع، افزایش سرمایه، عرضه اولیه) در راه است.
- [ ] **مقیاس:** `marketPulse` برای هر فراخوانی metrics را دوباره حساب می‌کند (O(n))؛ برای ۷۳۰ نماد ~۳۰ms قابل‌قبول است اما بهتر است یک‌بار در `rank` کش شود.

## لایه ۸ — نتیجه‌گیری

نسخه ۲.۱ یک **ویترین** بود: ظاهر حرفه‌ای، اعداد ساختگی درون یک موتور که پایتون نداشت. نسخه ۳.۰ همان هویت بصری را نگه داشته اما سه چیز را عوض کرده است:
۱) **قواعد بازار ایران** وارد مدل شد (دامنه، مبنا، صف، پول حقیقی، کد‌به‌کد، صنعت، شناوری)؛
۲) **صداقت داده** به یک قرارداد فنی تبدیل شد (هیچ عدد ساختگی، برچسب منبع/زمان، اطمینان به‌جای اطمینان کاذب)؛
۳) **مهندسی**: هسته قابل‌آزمون، پروکسی امن، پایپ‌لاین واقعی پایتون و برابری دو پیاده‌سازی.

*تولیدشده در Arena Agent Mode — ۳۱ شهریور ۱۴۰۵*
