# پیشنهادهای خلاقانه — وضعیت و مسیر بعدی

> نسخه ۱ این فایل (پیش از تحول) هفت ایده کم‌تلاش/پراثر پیشنهاد می‌داد.
> در نسخه ۳.۰ شش مورد از هفت ایده پیاده شده و ده ایده تازه بر پایه **قواعد بازار ایران** افزوده شده است.
> ستون «چرا ایرانی؟» یعنی این قابلیت بدون شناخت تالار تهران معنا ندارد.

## الف) وضعیت پیشنهادهای نسخه ۱

| # | ایده | وضعیت | کجا |
|---|---|---|---|
| ۱ | نبض ۶۰ ثانیه‌ای (Live Pulse) | ✅ پیاده (کامل‌تر از متن اولیه) | `#pulse` — دمای بازار، عرض بازار، ارزش معاملات، پول حقیقی خالص، سقف/کف‌چسبیده، میانگین هم‌وزن |
| ۲ | هیت‌مپ صنایع | ✅ پیاده | تب «هیت‌مپ صنایع» — رنگ = میانگین تغییر، اندازه = ارزش معاملات، کلیک = فیلتر جدول |
| ۳ | اسلایدر What-If وزن‌ها | ✅ پیاده (+ آستانه‌های وتو) | `#w-sliders` و `#r-*` — بازمحاسبه زنده در مرورگر + ذخیره در localStorage |
| ۴ | ترمینال قابل‌تایپ (CLI) | ✅ پیاده (+۳ دستور v3.1) | `#term-input` — `help · top · sort · filter · sector · explain · watch · weights · rules · export · alert · compare · report · refresh · clear` |
| ۵ | تور ۹۰ ثانیه‌ای | ✅ پیاده (بدون کتابخانه) | دکمه «تور» در ناوبری / کلید `t` — ۸ گام spotlight (کارنامۀ مدل افزوده شد) |
| ۶ | اشتراک تلگرام/واتساپ | ✅ پیاده | دکمه «اشتراک» کنار CSV — پیام فارسی با نمادها، امتیاز، حد ضرر و لینک |
| ۷ | بوم «شبکه عصبی» هیرو | ✅ پیاده (v3.1) | دکمۀ «بوم: شبکه عصبی» — حالت دوم با معماری ورودی→پنج‌عامل→درجه؛ کندل‌استریمر حفظ شد؛ برچسب «تصویرسازی، نه مدل آموزشی فعال» |

## ب) افزوده‌های جدید این نسخه (ایده‌هایی که خودِ بازار دیکته کرد)

| ایده | شرح | «چرا ایرانی؟» |
|---|---|---|
| **دمای بازار (۰–۱۰۰)** | ترکیب عرض بازار، قدرت خرید کل، شیب وزننی و نسبت صف خرید به فروش | در بازار صف‌محور، دمای عددی به‌تنهاییِ «شاخص کل» گمراه‌کننده است |
| **ظرفیت تا سقف مجاز** | نوار درصد باقی‌مانده از دامنه روزانه + تنبیه امتیاز کوتاه‌مدت | ±۳٪ یعنی «امروز» و «فردا» دو دنیای متفاوت‌اند |
| **رادار صف‌ها** | سنگین‌ترین صف‌های خرید با ارزش ریالی و تعداد برگه | قفل بودن صف در تالار ایران شایع‌ترین دامع/فرصت است |
| **رادار کد‌به‌کد** | تفکیک حقوقی→حقیقی، حقیقی→حقوقی، جمع‌آوری و خروج + یادداشت «حدس ساختاری» | کد‌به‌کد در ایران یک سیگنال شناخته‌شده است و در هیچ مدل وارداتی نیست |
| **ضریب اطمینان** | پوشش داده × هم‌راستایی پنج عامل؛ سقف درجه با داده ناقص | منبع داده ایرانی گاهی فیلد کم دارد؛ مدل باید اعتراف کند نه اختراع |
| **حالت آفلاین صادقانه** | برچسب «اسنپ‌شات آفلاین» + دلیل‌ها + disclaimer داده نمونه در UI | فیلترینگ/قطعی، بخشی از تجربه عادی کاربر ایرانی است |
| **پروکسی کلیددار در سرور** | `server.py` بدون وابستگی، TTL cache، `X-Data-Source` | کلید BrsApi در مرورگر = نشت در ریپوی عمومی |
| **شمارش معکوس جلسه/TAL** | «پیش‌گشایش · بازار باز · حراج پایانی» + زمان تا بازگشایی | TAL و پیش‌گشایش در ساعت تالار معنادار است |
| **نقشه معامله با «جلسه لازم»** | اهداف ATR-محور + `sessionsToTarget = ceil(% / limit)` | در دامنه ۳٪، «سه پله تا هفته بعد» واقع‌بینانه‌تر از «هدف ۲۰٪» است |
| **کارت‌ویو موبایل از همان جدول** | `data-l` + CSS، بدون رندر دوباره | کاربر ایرانی بیش از ۶۰٪ موبایل |
| **دیده‌بان شخصی (ستاره)** | ذخیره در localStorage، نمایش در «روی رادار فردا» | چرخه تصمیم دو‌جلسه‌ای در بازار صفی |
| **خروجی CSV و JSON یک‌کلیک** | از همان ردیف‌های فیلترشده | تحویل به اکسل/ره‌آورد عادت تحلیل‌گر ایرانی است |

## ج) بعدی‌ها — **تحویل‌داده‌شده در نسخه ۳.۱** (۲۲ شهریور ۱۴۰۵)

| هفته | تحویل | وضعیت | کجا |
|---|---|---|---|
| ۱ | **بک‌تست پایداری سیگنال:** ثبت هر اسکن در `out/runs/` + ارزیابی ۱/۳/۵ جلسه‌ای (Hit-rate، بازده، بازده به ریسک) + کارت «کارنامۀ مدل» | ✅ | `tsepy/backtest.py` · `main.py --backtest` → `data/model-report.json` · `assets/js/scorecard.js` · `#scorecard` |
| ۲ | **کارت اتفاقات نماد:** مجمع، افزایش سرمایه، بازگشایی، عرضه اولیه، افصاری | ✅ (نمونۀ برچسب‌دار) | `tools/make_offline_snapshot.py` → `data/events.json` · `assets/js/events.js` · ردیف جدول + پنل جزئیات |
| ۳ | **تاریخچۀ کامل بازار:** کش دیسکی `data/history/{key}.json` با TTL هفتگی | ✅ | `server.py` → `history_lookup()` (mem→disk→net→stale) |
| ۴ | **شبکۀ عصبی هیرو** + **هشدار تلگرامی شرطی** + **مقایسۀ دو نماد** | ✅ | `assets/js/app.js#heroCanvas` (دو حالت) · `assets/js/alerts.js` (`alert add …`) · `assets/js/compare.js` (`compare a b`) |

### معیار موفقیت — وضعیت
- ✅ بک‌تست cut-off/no-lookahead با ۲۲ خودآزمون (`tools/backtest_check.py`) و ۱۵ آزمون JS (`tests/lab.test.mjs`) قفل شد.
- ✅ کارنامۀ مدل: Hit-rate سه‌جلسه‌ای روی شبیه‌سازی ≈ ۲۱٪ (کمتر از ۵۰٪) اما **lift ≈ +5pp و بازده مازاد ≈ +0.5٪ مثبت** → داور «قابل‌قبول/ارزش‌افزوده»؛ داوری سه‌ستونه (نه فقط آستانۀ مطلق) تا بسامد پایینِ اهدافِ ATR در بازار صفی به‌اشتباه «ضعیف» خوانده نشود.
- ⏳ پوشش تاریخچۀ زندۀ ≥۹۰٪: منوط به `BRS_API_KEY`/شبکه؛ کش دیسکی آن را در اجرای دوم و بعد رایگان می‌کند.
- ✅ زمان نخستین رندر: اسنپ‌شات محلی پیش از هر fetch رندر می‌شود (زیر ۴۰۰ms).

---

### یادداشت بصری حفظ‌شده از نسخه ۱
پالت `#16e08c · #4cc9ff · #9b8cff · #ffc14d · #ff5470` روی `#04070d`، Vazirmatn برای فارسی و JetBrains Mono برای اعداد؛
همه آیکون‌ها `stroke-width` یکسان؛ اعداد همیشه `dir=ltr` — در نسخه ۳.۰ با حذف Tailwind CDN این سیستم به `assets/css/theme.css` منتقل شد
و کلاس‌های تولیدشده در رندر هم با آزمون (`dom.test.mjs#۸`) به CSS قفل شده‌اند تا استایلِ‌بی‌کلاس باقی نماند.
