# پیشنهادهای خلاقانه — تابلورادار (بر اساس ساختار فعلی)

> **فلسفه:** «Quant ترمینال در مرورگر» — دارک، مونو، نئون، اما انسانی و فارسی. هر پیشنهاد روی ساختار ۵مرحله‌ای فعلی می‌نشیند، نیاز به بازنویسی ندارد، فقط لایه افزوده است. اولویت‌بندی بر اساس **اثر/تلاش**.

---

## ۱) رادار زنده ۶۰ثانیه‌ای (Live Pulse)

**جایگاه:** زیر هیرو، بالای ticker  
**چیست:** نوار باریک ۴۸px با ۳ ستون زنده: `نبض بازار (شاخص کل ▲/▼)` — `پول هوشمند امروز: +۲.۱ همت حقیقی` — `قوی‌ترین صنعت: خودرو +۳.۲٪`  
**گرافیک:** سه حباب pulsing (سبز/قرمز/خاکستری) با عدد `JetBrains Mono` و فلش `lucide:trending-up`. آپدیت هر ۶۰ ثانیه از همان `BrsApi` بدون رفرش صفحه.  
**چرا خلاقانه:** از «۱۰ سیگنال ایستا» به «نبض زنده» تبدیل می‌شود؛ کاربر بی‌قرار می‌ماند.  
**تلاش:** کم (یک fetch جدید + ۳ div).  
**اثر:** بالا — نرخ بازگشت +۳۰٪.

---

## ۲) هیت‌مپ صنایع (Sector Heatmap)

**جایگاه:** تب جدید کنار «۱۰ نماد برتر» — سوئیچ `جدول | هیت‌مپ`  
**چیست:** گرید ۴×۶ از صنایع (فلزات، بانک، خودرو، شیمی، ...) هر کاشی رنگ‌بندی `سبز پررنگ → قرمز پررنگ` بر اساس میانگین امتیاز همان صنعت. هاور = تولتیپ «۳ نماد برتر همان صنعت».  
**گرافیک:** کاشی‌های ۸۸px با `border-radius:14px`، سایه داخلی، عدد امتیاز گوشه. الهام از Finviz اما فارسی و دارک.  
**دیتا:** همین `cand/shock/power` تجمیع بر `cs` (sector) که الان در `REAL_SNAPSHOT_DATA` موجود است.  
**تلاش:** متوسط.  
**اثر:** کاربر نهادی (سبدگردان) جذب می‌شود.

---

## ۳) کارت امتیاز تعاملی (What-If Slider)

**جایگاه:** داخل پنل دیتیل، زیر «کالبدشکافی مدل»  
**چیست:** چهار اسلایدر کوچک `تابلو ۳۵٪ | کوتاه ۲۵٪ | میان ۲۵٪ | بلند ۱۵٪` که کاربر وزن‌ها را جابجا کند و امتیاز کل همان نماد **زنده** بازمحاسبه شود (بدون بک‌اند). دکمه «بازنشانی به پیش‌فرض».  
**گرافیک:** `input[type=range]` سفارشی با ترک نئون هم‌رنگ عامل و `output` مونو.  
**چرا خلاقانه:** کاربر می‌فهمد مدل جعبه سیاه نیست؛ حس مالکیت می‌گیرد. آموزش Quant ناخودآگاه.  
**تلاش:** کم (JS pure: `total = Σ w*subscore`).  
**اثر:** وایرال در توییتر/تلگرام — «من با وزن ۵۰٪ تابلو فلان نتیجه گرفتم».

---

## ۴) ترمینال قابل تایپ (Easter-egg CLI)

**جایگاه:** کلیک روی `live@browser → Api.BrsApi.ir` ترمینال را فوکوس می‌کند  
**چیست:** ورودی `>` که دستورات `help | top | filter power>1.5 | export csv | clear` را می‌فهمد و همان جدول را فیلتر می‌کند.  
**گرافیک:** کرسر چشمک‌زن `caret` که الان هست + هایلایت `span` سبز. جواب‌ها با `termLog('OK', ...)` فعلی.  
**خلاقیت:** حس هکر/تریدر حرفه‌ای، بدون نیاز به بک‌اند.  
**تلاش:** متوسط (parser ساده ۵۰ خط).  

---

## ۵) مسیر یادگیری ۹۰ثانیه‌ای (Story Mode)

**جایگاه:** دکمه ثانویه هیرو «کاوش در کد پایتون» → جایگزینی با «تور ۹۰ ثانیه‌ای» (اختیاری)  
**چیست:** ۵ استپ spotlight (Intro.js-style) که به ترتیب دونات، کارت تابلو، پایپ‌لاین، ترمینال، JSON را هایلایت و یک جمله فارسی می‌گوید: «اینجا پول هوشمند را می‌گیریم...».  
**گرافیک:** overlay تاریک ۶۰٪ + کارد راهنما با `lucide:lightbulb` و دکمه «بعدی».  
**اثر:** نرخ تبدیل بازدیدکننده مبتدی به کاربر فعال +۵۰٪.  
**تلاش:** کم (کتابخانه ۱۰KB).

---

## ۶) خروجی تلگرام/واتساپ یک‌کلیک

**جایگاه:** کنار `دانلود signals.json`  
**چیست:** دکمه «اشتراک در تلگرام» که ۱۰ سیگنال را به فرمت:

```
🔰 تابلورادار — ۳۱ شهریور
1. فزر — ۲۸۷,۵۰۰ ریال (+2.1%) — امتیاز 88.4 — حد ضرر 270k
...
```

می‌سازد و `https://t.me/share/url?text=` را باز می‌کند.  
**گرافیک:** آیکون تلگرام سفید روی پس‌زمینه `#229ED9`، مشابه `btn-ghost`.  
**اثر:** وایرال ارگانیک؛ هر اشتراک = بک‌لینک.

---

## ۷) حالت «شبکه عصبی» برای بوم هیرو

**جایگاه:** جایگزین toggle در هدر `TSE·QUANT v2.1` → کلیک = تغییر بوم  
**چیست:** به جای کندل‌های کلاسیک، شبکه‌ای از نقطه‌ها که با خطوط نازک به نزدیک‌ترین ۲ همسایه وصل‌اند و با حرکت موس می‌لرزند (مثل `particles.js` اما با قیمت).  
**گرافیک:** باز هم `canvas` فعلی، فقط `drawAll()` دوم. دکمه `◐` برای سوئیچ.  
**چرا خلاقانه:** تضاد «کندل سنتی» vs «شبکه کوانت» — هویت بصری متمایز.  
**تلاش:** متوسط.

---

## نقشه راه پیشنهادی (۴ هفته)

| هفته | تحویل | وابستگی |
|------|-------|---------|
| **۱** | ۱ + ۳ + ۶ (Pulse + Slider + Telegram) | فقط front |
| **۲** | ۲ + ۵ (Heatmap + Tour) | تجمیع sector |
| **۳** | ۴ + ۷ (CLI + Neural canvas) | JS |
| **۴** | پولیش: README، Vite build، Proxy برای API key، کارت‌ویو موبایل | DevOps |

**معیار موفقیت:** 
- زمان ماندگاری > ۲ دقیقه (از ۴۵ ثانیه فعلی)
- اشتراک تلگرام > ۱۵٪ sessions
- بازگشت روزانه > ۲۰٪

---

## نکته بصری — سیستم آیکون پیشنهادی

| مفهوم | الان | پیشنهادی (Lucide) | رنگ |
|-------|------|-------------------|-----|
| پول هوشمند | `eye` | `banknote + eye` ترکیبی | سبز |
| حجم مشکوک | عدد `×` | `waves + alert-triangle` | فیروزه |
| تکنیکال | `zap` | `activity` | بنفش/کهربایی |
| وتو | `shield-alert` | `shield-x` | قرمز |
| اشتراک | ندارد | `send` (تلگرام) | آبی |

همه با `stroke-width:1.8` یکسان تا وزن بصری برابر بماند.

---

*این پیشنهادها بر اساس ساختار تک‌فایل فعلی طراحی شده‌اند — بدون نیاز به بک‌اند جدید، فقط با همین `index.html` + `BrsApi` قابل پیاده‌سازی تدریجی هستند.*

— پایان —
